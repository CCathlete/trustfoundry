/** @type {import('tailwindcss').Config} */
export default {
    // The 'content' array specifies which files to scan for Tailwind utility classes.
    content: [
        "./index.html",
        "./src/**/*.{js,ts,jsx,tsx}",
    ],
    theme: {
        extend: {},
    },
    plugins: [],
}